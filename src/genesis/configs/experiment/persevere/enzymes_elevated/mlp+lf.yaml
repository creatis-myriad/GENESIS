# @package _global_

# to execute this experiment run:
# python train.py experiment=persevere/enzymes_elevated/mlp+lf

defaults:
  - /experiment/persevere/enzymes_elevated/_base
  - override /model: graph_level_late_fusion
  - override /model/encoder: mlp

  # Use the default config, and only define hparams tuned for the dataset+model using automated hyperparameter search
  - override /model/readout: mean

model:
  encoder:
    hidden_channels: 16
    num_layers: 4
    dropout: 0.028
    norm: BatchNorm
  head:
    num_layers: 3
  optimizer:
    weight_decay: 1.8e-5
