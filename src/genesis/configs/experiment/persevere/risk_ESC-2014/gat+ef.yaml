# @package _global_

# to execute this experiment run:
# python train.py experiment=persevere/risk_ESC-2014/gat+ef

defaults:
  - /experiment/persevere/risk_ESC-2014/_base
  - override /model: graph_level_early_fusion
  - override /model/encoder: gat

  # Use the default config, and only define hparams tuned for the dataset+model using automated hyperparameter search
  - override /model/readout: last

model:
  encoder:
    hidden_channels: 64
    num_layers: 2
    dropout: 0.058
    norm: BatchNorm
  head:
    num_layers: 1
  optimizer:
    weight_decay: 4.8e-5
