# @package _global_

# to execute this experiment run:
# python train.py experiment=persevere/risk_ESC-2014/gcn+gavn

defaults:
  - /experiment/persevere/risk_ESC-2014/_base
  - override /model/transforms: graph_attr_virtual_nodes
  - override /model/encoder: gcn

  # Use the default config, and only define hparams tuned for the dataset+model using automated hyperparameter search
  - override /model/readout: sum

model:
  encoder:
    hidden_channels: 32
    num_layers: 2
    dropout: 0.13
    norm: BatchNorm
  head:
    num_layers: 2
  optimizer:
    weight_decay: 1.1e-5
