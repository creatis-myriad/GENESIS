# @package _global_

# to execute this experiment run:
# python train.py experiment=persevere/risk_ESC-2014/gat

defaults:
  - /experiment/persevere/risk_ESC-2014/_base
  - override /model/encoder: gat

  # Use the default config, and only define hparams tuned for the dataset+model using automated hyperparameter search
  - override /model/readout: sum

model:
  encoder:
    hidden_channels: 64
    num_layers: 2
    dropout: 0.25
    norm: BatchNorm
  head:
    num_layers: 3
  optimizer:
    weight_decay: 2.5e-4
