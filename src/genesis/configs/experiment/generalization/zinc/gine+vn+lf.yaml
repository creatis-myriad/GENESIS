# @package _global_

# to execute this experiment run:
# python train.py experiment=generalization/zinc/gine+vn+lf

defaults:
  - /experiment/generalization/zinc/gine+vn
  # Use the GINE+VN config, modified to:
  # 1) compute molecular global features, and;
  # 2) concatenate the global features to graph representation from GNN encoder to perform late fusion

  - override /data/dataset/transform@data.dataset.pre_transform: add_molecular_global_features
  - override /model: graph_level_late_fusion
