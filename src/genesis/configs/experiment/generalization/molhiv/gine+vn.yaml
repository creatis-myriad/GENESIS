# @package _global_

# to execute this experiment run:
# python train.py experiment=generalization/molhiv/gine+vn

defaults:
  - /experiment/generalization/molhiv/_gine_base
  # Use the base GINE config, modified to:
  # 1) use randomly initialized virtual nodes

  # Virtual node must come after atom and bond embeddings, so that it can infer node/edge features dimensions
  - /data/dataset/transform@model.transforms.virtual_node: virtual_node
