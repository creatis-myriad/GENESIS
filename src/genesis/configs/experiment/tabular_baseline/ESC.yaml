# @package _global_

# to execute this experiment run:
# python tabular_baseline.py experiment=tabular_baseline/ESC

defaults:
  - tabular_baseline/_base

# Use the base config, and only define which set of columns/attributes to use from the dataset
data:
  dataset:
    usecols:
      - patient_id
      - risk_ESC-2014
      - spesi
      - nt-probnp
      - troponin
      - inverted_rv_lv_ratio_radiologist
