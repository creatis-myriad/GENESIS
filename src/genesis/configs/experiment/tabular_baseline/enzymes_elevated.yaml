# @package _global_

# Tabular baseline configuration for binary classification of whether blood biomarkers, i.e. enzymes, are elevated
# on the PERSEVERE dataset.

defaults:
  - /experiment/tabular_baseline
  - override /data/dataset/target: enzymes_elevated
  - override /model/metrics: binary_classification
