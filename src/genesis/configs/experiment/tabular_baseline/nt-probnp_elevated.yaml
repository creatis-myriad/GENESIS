# @package _global_

# Tabular baseline configuration for binary classification of whether NT-proBNP, an enzyme measured in the blood, is elevated
# on the PERSEVERE dataset.

defaults:
  - /experiment/tabular_baseline
  - override /data/dataset/target: nt-probnp_elevated
  - override /model/metrics: binary_classification
