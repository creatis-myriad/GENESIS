# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/graph_level_features_embedding/gcn+gaef-tabpfn

# Use the default GCN+GAEF config, modified to:
# - replace graph-level features with their TabPFN embedding
defaults:
  - /experiment/persevere/risk_ESC-2014/gcn+gaef
  - override /data/dataset/transform@data.dataset.pre_transform: tabpfn_embedding
