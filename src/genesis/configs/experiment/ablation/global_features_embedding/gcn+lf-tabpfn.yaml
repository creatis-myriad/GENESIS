# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/global_features_embedding/gcn+lf-tabpfn

# Use the default GCN+LF config, modified to:
# - replace global features with their TabPFN embedding
defaults:
  - /experiment/persevere/risk_ESC-2014/gcn+lf
  - override /data/dataset/transform@data.dataset.pre_transform: tabpfn_embedding
