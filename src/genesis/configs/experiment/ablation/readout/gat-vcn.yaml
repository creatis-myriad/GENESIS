# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/readout/gat-vcn

# Use the default GAT-VCN config:
# - readout extracting the output virtual node representation
defaults:
  - /experiment/persevere/enzymes_elevated/gat-vcn

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
