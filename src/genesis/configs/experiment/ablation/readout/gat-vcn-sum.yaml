# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/readout/gat-vcn-sum

# Use the default GAT-VCN config, modified to:
# - change readout to sum all node representations
defaults:
  - /experiment/persevere/enzymes_elevated/gat-vcn
  - override /model/readout: sum

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
