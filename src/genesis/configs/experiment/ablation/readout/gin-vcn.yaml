# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/node_features/gin-vcn

# Use the default GIN-VCN config (virtual node initialized w/ graph-level features + readout extracting the virtual node)
defaults:
  - /experiment/persevere/risk_ESC-2014/gin-vcn

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
