# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/readout/gin-vcn

# Use the default GIN-VCN config:
# - readout extracting the output virtual node representation
defaults:
  - /experiment/persevere/enzymes_elevated/gin-vcn

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
