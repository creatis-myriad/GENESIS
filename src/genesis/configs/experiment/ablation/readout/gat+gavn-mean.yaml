# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/readout/gat+gavn-mean

# Use the default GAT-GraphAttrVN config, modified to:
# - set readout to average all node representations
defaults:
  - /experiment/persevere/enzymes_elevated/gat+gavn
  - override /model/readout: mean

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
