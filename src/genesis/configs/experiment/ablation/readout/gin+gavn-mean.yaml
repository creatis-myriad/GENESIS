# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/readout/gin+gavn-mean

# Use the default GIN+GraphAttrVN config, modified to:
# - change readout to average all node representations
defaults:
  - /experiment/persevere/enzymes_elevated/gin+gavn
  - override /model/readout: mean

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
