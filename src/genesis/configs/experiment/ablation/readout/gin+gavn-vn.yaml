# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/readout/gin+gavn

# Use the default GIN+GraphAttrVN config:
# - set readout to extract the output virtual node representation
defaults:
  - /experiment/persevere/enzymes_elevated/gin+gavn
  - override /model/readout: last

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
