# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/graph_representation+features/dual-gat-vcn

# Use the default Gat-VCN config:
# - dual representation
# - node features
# - no positional encoding
# - virtual nodes initialized w/ graph-level features
defaults:
  - /experiment/persevere/enzymes_elevated/gat-vcn

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
