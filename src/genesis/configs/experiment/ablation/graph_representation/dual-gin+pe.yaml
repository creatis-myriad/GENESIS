# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/graph_representation/dual-gin+pe

# Use the default GIN config (dual representation w/o positional encoding), modified to:
# - add PE
defaults:
  - /experiment/persevere/enzymes_elevated/gin
  - override /data/dataset/transform@data.dataset.transform: random_walk_pe

hparams:
  data:
    pe_attr: null # Concatenate positional encoding to node features

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
