# @package _global_

# to execute this experiment run:
# python train.py experiment=ablation/graph_representation/dual-gin

# Use the default GIN config (dual representation w/o positional encoding)
defaults:
  - /experiment/persevere/risk_ESC-2014/gin

logger:
  wandb:
    project: "GENESIS-ablation" # set a dedicated project for ablation studies
