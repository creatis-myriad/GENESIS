# @package _global_

data:
  dataset:
    target_attr: risk_ESC-2014

hparams:
  data:
    num_classes: 3
    classes_rescale_weights: # Weights by which to rescale each class to counteract class imbalance
      _target_: torch.tensor
      _args_:
        - [1.6, 1., 1.1]
    task: multiclass
