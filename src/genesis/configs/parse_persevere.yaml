# @package _global_

defaults:
  - _self_
  - paths: default
  - hydra: default
  - data/dataset@clinical_data: persevere_clinical

source_dir: ${paths.data_dir}/PERSEVERE/seg2graphs
pyg_raw_dir: ${paths.data_dir}/PERSEVERE/raw

attrs_to_remove:
  nodes: [pos]
  links: [id, name, lateral, lobe, centerline, radius, contour_points]
