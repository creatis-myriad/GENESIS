# @package _global_

defaults:
  - _self_
  - paths: default
  - hydra: default

source_dir: "${paths.data_dir}/PERSEVERE/seg2graphs"
pyg_raw_dir: "${paths.data_dir}/PERSEVERE/raw"

graph_attrs:
  csv_file: "PERSEVERE_selection.csv"
  patient_id_column: 0
  attrs_to_extract:
    vte_severity: 3

attrs_to_remove:
  node: ["pos"]
  link:
    ["id", "name", "lateral", "lobe", "centerline", "radius", "contour_points"]
